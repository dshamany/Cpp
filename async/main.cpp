#include <iostream>
#include <string>
#include <boost/asio.hpp>

int main(int argc, char const *argv[])
{
    boost::system::error_code ec;
    boost::asio::io_context io_context;

    // server endpoint required for connect
    // socket.connect()

    return 0;
}
